name: ui_module
description: A new Flutter project.

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  function_module:
    git:
      url: https://github.com/gokulsundar188/function_module
      ref: develop

  theme_module:
    git:
      url: https://github.com/gokulsundar188/theme_module
      ref: develop
  cupertino_icons: ^0.1.3
  toast: ^0.1.5

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
